admittance_controller:
  conflicting_inclusive_bounds:
    type: int
    default_value: 16
    description: "should be a number between 10 and 20"
    validation:
      gt_eq<>: [ 10 ]
      lt_eq<>: [ 20 ]
